/* import createPersistedState from 'vuex-persistedstate'
import { usersCollection } from '../plugins/firebase.js'
import firebase from 'firebase'

export default ({store}) => {
  // window.onNuxtReady(() => {
  //   createPersistedState({
  //       paths: ['user', 'userId', 'username'],
  //       key: 'vuex',
  //   })(store)
  // })
} */

export default () => {}
